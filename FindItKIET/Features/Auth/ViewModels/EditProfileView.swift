// EditProfileView.swift
import SwiftUI

struct EditProfileView: View {
    var body: some View {
        Form {
            Section(header: Text("Edit Profile")) {
                Text("This is a placeholder. Add fields for name, email, etc.")
            }
        }
        .navigationTitle("Edit Profile")
    }
}
