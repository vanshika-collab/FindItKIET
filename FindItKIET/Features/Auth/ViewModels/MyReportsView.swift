// MyReportsView.swift
import SwiftUI

struct MyReportsView: View {
    var body: some View {
        List {
            Section {
                Text("My Reports placeholder")
                Text("Show items created by the current user here.")
            }
        }
        .navigationTitle("My Reports")
    }
}
